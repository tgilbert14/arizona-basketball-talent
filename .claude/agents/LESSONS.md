# Suite lessons (append-only; cold-boot agents read this first)

- [2026-07-12] gwaihir · over-flagged · OneDrive files-on-demand can show a 48-byte stub size in `ls` for a just-encoded file — `stat`+`ffprobe` before declaring an encode broken (old-pueblo conn3-m.mp4 was fine, 1.86MB/5.04s).
- [2026-07-12] gwaihir · gap · old-pueblo-scroll index.html:96,102 reference conn4.mp4/conn4-m.mp4 which were never rendered (avenue→kittpeak seam 404s); renders are also uncommitted and the repo has no remote — "finished" ≠ shipped. Verify assets-on-disk vs page config before calling a scroll world done.
- [2026-07-13] lindir · confirmed · eyebrow timestamps can carry a factual claim silently — Mt Lemmon brief spans 7:04→8:01 AM so the scroll itself proves "under an hour" with zero copy spend; when the subject's hook is a duration/speed claim, encode it in the timestamps and free the words for the reframe (gate: 0 findings first pass).
- [2026-07-13] lindir · confirmed · check_story_brief.py's monotonic check only fires on an HH:MM AM/PM substring inside the eyebrow, so a non-Saturday arc (e.g. a <1hr road-trip climb) can layer elevation/mile-marker numbers alongside a real clock time in the same eyebrow string without tripping C8 — and folding the dip + ten reframe into one scene (Old Pueblo's Kitt Peak trick) again passed clean at 5 scenes, keeping a 4-6 constraint piece under budget while still satisfying the schema's required dip role.
- [2026-07-13] gwaihir · confirmed · a Story Brief's own shot verbs can be filter tripwires — Mt Lemmon brief said "strobing past", "speed breaks over the ridge", "green Mile 0 marker" (a numeral = no-text leak); transliterate brief language through the Seedance lexicon (streaming/crests/blank marker post) instead of copying it, and generating all prompt files from one Python constant makes preamble byte-identity structural rather than checked (lint gate then passed 0/0 first run).
